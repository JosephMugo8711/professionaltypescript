// here i have created a function sum
// takes two arguments x, y and the types can only be numbers
// (...): number means that the return value can only be a number

function sum(x: number, y: number):  number {
    return x + y
}

//sum(84 + 76) // will be return 160

// function sum1(x: string, y: string): number {
//     // return x + y;
// }

// function sum2(x: number, y: number): string {
//     // return x + y;
//  }

 // error TS2322: Type 'string' is not assignable to type 'number' and error TS2322: Type 'number' is
// not assignable to type 'string' respectively

