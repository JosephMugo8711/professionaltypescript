// while some existing javascript library have type definition files, there are many that don't
// TypeScript offers a couple patterns to handle missings declarations 

// Make a module that exports a default any
// For more complicated projects, or in cases where you intend to gradually type a dependency, it may be cleaner to create a module
// UsingJquery (although it does have typings available) as an example:


