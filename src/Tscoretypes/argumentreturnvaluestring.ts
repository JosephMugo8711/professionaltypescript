function hello(name: string): string {
    return `Hello ${name}!`;
}

// accepts one name argument and this argument can only be  a string
// (...): string { means that the return value can only be a string

hello('StackOverflow Documentation') // will be return Hello StackOverflow Documentation!